
execution:
  - concurrency: 30
    ramp-up: 1m
    hold-for: 5m
    scenario: quick-test
scenarios:
  quick-test:
    requests:
      - url: 'https://amer-poc6-prod.apigee.net/currency/v1/logs'
        method: GET
